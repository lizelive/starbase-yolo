include "trig_advanced"
include "config"
include "std/logic"
include "std/math"

// resting position
:TargetZ=1000
:TargetX=0
:TargetY=0

logic_wait(not :Active)
t=0
while :Active do
    t+=Speed
    o=trig_mod(t)
    math_sin(x,o)
    x*=Scale*t
    math_cos(y,o)
    y*=Scale*t
    
    z=:RangeFinderDistance
    :TargetX=x
    :TargetY=y
    :TargetZ=z
end
