// use global time
macro now()<Clock_Time> expr
	Clock_Time
end

macro dtime(out)<Clock_Time, lasttime> block
	out=lasttime-now()
	lasttime+=out
end

include "std/math"

macro time_format(out, seconds) block
math_mod()
end